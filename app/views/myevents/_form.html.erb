<%= form_for(@myevent) do |f| %>
  <% if @myevent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@myevent.errors.count, "error") %> prohibited this myevent from being saved:</h2>

      <ul>
      <% @myevent.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :athlete_id %><br />
    <%= f.number_field :athlete_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
